<div class="row">
  <div class="container">
    <div class="row">
      <div class="col-sm-10 col-sm-offset-1">
        <h2 class="text-center">ホームページ一覧</h2>
        <div class="row">
          <% if @homepages.present? %>
            <% @homepages.each do |homepage| %>
              <div class="col-sm-4 card">
                <h4><%= homepage.title %></h4>
                <div class="homepage-image"></div>
                <p class="text-center"><%= link_to "VIEW", homepage_path(homepage) %></p>
              </div>
            <% end %>
          <% else %>
            まだホームページはございません。
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>